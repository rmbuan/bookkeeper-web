# Database (when Postgres runs via: cd web && docker-compose up -d)
DATABASE_URL="postgresql://bookkeeper:bookkeeper@localhost:5432/bookkeeper"

# Server
PORT=4000

# Optional
# FRONTEND_ORIGIN=http://localhost:5173
# SESSION_SECRET=your-random-secret
